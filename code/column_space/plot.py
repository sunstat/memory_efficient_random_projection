import matplotlib.pyplot as plt

def plot(tensor_dim, mode, res):
    '''
    generate two plots, one for dense tensor, the other for low rank square tensor;
    '''
    fig1, ax1 = plt.subplots()
    pass
